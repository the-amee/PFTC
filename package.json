{
  "name": "perfect-family-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site (HTML/CSS/JS). No npm dependencies.",
  "scripts": {
    "dev": "python3 -m http.server 3000"
  }
}
